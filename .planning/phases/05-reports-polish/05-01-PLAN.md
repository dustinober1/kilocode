---
phase: 05-reports-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
    - cli/src/services/analytics/ReportGenerator.ts
    - cli/src/services/analytics/__tests__/ReportGenerator.test.ts
autonomous: true

must_haves:
    truths:
        - "User can export all analytics data to JSON format"
        - "Export includes sessions, events, and sanitized PII data"
        - "Export respects privacy configuration (no sensitive data leaks)"
        - "Export format is valid JSON with consistent structure"
    artifacts:
        - path: "cli/src/services/analytics/ReportGenerator.ts"
          provides: "Data export service for JSON generation"
          min_lines: 80
          exports: ["generateSessionReport", "generateFullExport", "exportToJsonFile"]
        - path: "cli/src/services/analytics/__tests__/ReportGenerator.test.ts"
          provides: "Test coverage for export functionality"
          min_lines: 60
    key_links:
        - from: "ReportGenerator.ts"
          to: "StorageService"
          via: "getInstance() for database access"
          pattern: "StorageService\\.getInstance\\(\\)"
        - from: "ReportGenerator.ts"
          to: "PrivacyConfig"
          via: "Privacy sanitization in export"
          pattern: "hashPII|filterPrompts"
---

<objective>
Build ReportGenerator service for exporting analytics data to portable JSON format.

Purpose: Enable users to export their analytics data for backup, analysis, or migration to other tools. This completes the "data portability" requirement for the analytics system.

Output: ReportGenerator service with JSON export capabilities, respecting privacy configuration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@cli/src/services/analytics/types.ts
@cli/src/services/analytics/schema.ts
@cli/src/services/analytics/StorageService.ts
@cli/src/services/analytics/privacy/PrivacyConfig.ts
@cli/src/services/analytics/AggregationService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ReportGenerator service class</name>
  <files>cli/src/services/analytics/ReportGenerator.ts</files>
  <action>
    Create ReportGenerator service following singleton pattern (like StorageService, AggregationService):

    1. Class structure:
       - Private constructor, static getInstance()
       - Private db: StorageService instance reference
       - Private privacyConfig: PrivacyConfig reference

    2. Export types (define in file):
       ```typescript
       export interface SessionExport {
         sessionId: string
         startTime: string
         endTime: string | null
         totalTokens: number
         totalCost: number
         commandCount: number
         toolUsageCount: number
         exitReason: string | null
         events: MetricEventExport[]
       }

       export interface MetricEventExport {
         id: number
         eventType: string
         timestamp: string
         metadata: Record<string, unknown>
       }

       export interface AnalyticsExport {
         exportedAt: string
         version: string
         privacyConfig: {
           hashPII: boolean
           filterPrompts: boolean
         }
         sessions: SessionExport[]
       }
       ```

    3. Core methods:
       - generateSessionReport(sessionId: string): Promise<SessionExport>
         * Fetch session from sessions table
         * Fetch all events for session from metric_events
         * Parse metadata JSON to objects
         * Return SessionExport structure

       - generateFullExport(): Promise<AnalyticsExport>
         * Fetch all sessions (limit 100 most recent)
         * For each session, fetch events
         * Include privacy config snapshot
         * Return complete AnalyticsExport

    4. Do NOT create exportToFile method yet (will be in export command task)

    5. Use existing StorageService queries via db.select() with schema

    6. Respect privacy: Data coming from DB is already sanitized by PIISanitizer

  </action>
  <verify>
  File exists with:
  - ReportGenerator class with getInstance()
  - SessionExport, MetricEventExport, AnalyticsExport types
  - generateSessionReport() method
  - generateFullExport() method
  - Minimum 80 lines (excluding imports/comments)
  </verify>
  <done>
  ReportGenerator service created with data export methods. Can fetch sessions and events, structure them for JSON export.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add ReportGenerator tests</name>
  <files>cli/src/services/analytics/__tests__/ReportGenerator.test.ts</files>
  <action>
    Create comprehensive tests for ReportGenerator:

    1. Mock StorageService with vi.fn() for getInstance()
    2. Test scenarios (minimum 5 test cases):
       - generateSessionReport returns correct structure
       - generateSessionReport includes events array
       - generateFullExport includes privacy config
       - generateFullExport handles empty database
       - Export data types are correct (timestamps as ISO strings, numbers not strings)

    3. Use describe() blocks matching file structure:
       - describe('ReportGenerator') wrapper
       - describe('generateSessionReport') for session-specific tests
       - describe('generateFullExport') for full export tests

    4. Mock data: Create valid session and event objects matching schema types

    5. Pattern from existing tests: See AggregationService.test.ts for mocking StorageService

    6. Minimum 60 lines of test code

  </action>
  <verify>
  Run: pnpm --filter @kilocode/cli test ReportGenerator
  Expected: All tests pass, minimum 5 test cases, 60+ lines
  </verify>
  <done>
  ReportGenerator has comprehensive test coverage. Export methods validated with mock data.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add exportToJsonFile utility method</name>
  <files>cli/src/services/analytics/ReportGenerator.ts</files>
  <action>
    Add utility method to ReportGenerator for writing JSON to file:

    1. Method signature:
       ```typescript
       async exportToJsonFile(exportData: AnalyticsExport, filePath: string): Promise<void>
       ```

    2. Implementation:
       - Use Node.js fs.promises.writeFile
       - JSON.stringify with indent=2 for readable output
       - Wrap in try-catch, throw error on failure

    3. Add to class exports, update type exports if needed

    4. Follow async/await pattern, return Promise<void>

  </action>
  <verify>
  Method exists in ReportGenerator class with:
  - async exportToJsonFile(exportData, filePath) signature
  - Uses fs.promises.writeFile
  - JSON.stringify with pretty-print (indent: 2)
  </verify>
  <done>
  ReportGenerator can write export data to JSON file with proper formatting.
  </done>
</task>

</tasks>

<verification>
1. Run: pnpm --filter @kilocode/cli test ReportGenerator
   Expected: All tests pass

2. Lint: pnpm --filter @kilocode/cli lint
   Expected: No linting errors in ReportGenerator.ts

3. Type check: pnpm --filter @kilocode/cli check-types
   Expected: No TypeScript errors
   </verification>

<success_criteria>

- ReportGenerator service exists with singleton pattern
- Can export sessions and events to structured JSON
- Export includes privacy configuration metadata
- All tests pass with 5+ test cases
- Code follows existing service patterns (StorageService, AggregationService)
  </success_criteria>

<output>
After completion, create `.planning/phases/05-reports-polish/05-01-SUMMARY.md`
</output>
