---
phase: 05-reports-polish
plan: 03
type: execute
wave: 2
depends_on: [02-03]
files_modified:
    - cli/src/commands/stats.ts
    - cli/src/commands/privacy.ts
    - cli/src/commands/index.ts
autonomous: true

must_haves:
    truths:
        - "User can view current privacy settings via /privacy command"
        - "User can toggle individual privacy options (enabled, hashPII, filterPrompts)"
        - "Privacy changes persist to configuration"
        - "/privacy command shows current settings with clear descriptions"
    artifacts:
        - path: "cli/src/commands/privacy.ts"
          provides: "/privacy command for privacy settings management"
          min_lines: 60
        - path: "cli/src/commands/stats.ts"
          provides: "Updated with --history flag support"
          min_lines: 60
        - path: "cli/src/commands/index.ts"
          provides: "Command registration"
          exports: ["initializeCommands"]
    key_links:
        - from: "privacy.ts"
          to: "PrivacyConfig"
          via: "Reading/writing privacy configuration"
          pattern: "PrivacyConfig|DEFAULT_PRIVACY_CONFIG"
        - from: "privacy.ts"
          to: "MetricsCollectorService"
          via: "Updating privacy settings on collector"
          pattern: "MetricsCollectorService|updatePrivacyConfig"
        - from: "stats.ts"
          to: "AggregationService"
          via: "Fetching historical sessions"
          pattern: "getHistoricalSessions"
---

<objective>
Create privacy settings command and add history view to stats command.

Purpose: Expose privacy configuration to users for transparency and control. Add session history view for exploring past analytics data.

Output: Working /privacy command, enhanced /stats with --history flag.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@cli/src/commands/core/types.ts
@cli/src/commands/stats.ts
@cli/src/commands/index.ts
@cli/src/services/analytics/privacy/PrivacyConfig.ts
@cli/src/services/analytics/MetricsCollectorService.ts
@cli/src/services/analytics/AggregationService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /privacy command</name>
  <files>cli/src/commands/privacy.ts</files>
  <action>
    Create privacy command for viewing and managing privacy settings:

    1. Command structure:
       - name: "privacy"
       - aliases: ["privacy-settings", "analytics-privacy"]
       - description: "View and manage analytics privacy settings"
       - usage: "/privacy [list|set]"
       - examples: ["/privacy", "/privacy list", "/privacy set hashPII false"]
       - category: "settings"
       - priority: 5

    2. Add arguments for subcommands:
       - First arg: subcommand (list|set) - optional, defaults to "list"
       - For "set": second arg is setting name, third arg is value

    3. Handler implementation - "list" subcommand (default):
       - Get current PrivacyConfig from MetricsCollectorService
       - Display each setting with clear description:
         * enabled: "Analytics collection is currently ENABLED/DISABLED"
         * hashPII: "PII hashing is ENABLED/DISABLED - Usernames and paths are/aren't anonymized"
         * filterPrompts: "Prompt filtering is ENABLED/DISABLED - Sensitive patterns are/aren't redacted"
       - Use context.addMessage() for each line (formatted output)
       - Show path patterns count if available

    4. Handler implementation - "set" subcommand:
       - Parse args: [settingName, value]
       - Validate settingName (must be: enabled, hashPII, filterPrompts)
       - Parse value as boolean ("true", "false", "1", "0", "on", "off")
       - Update PrivacyConfig via MetricsCollectorService method (check if exists, add if needed)
       - Show confirmation: "Updated {setting} to {value}"
       - On invalid input: show error message with valid options

    5. Add private helper functions:
       - formatBooleanSetting(value: boolean, enabledText: string, disabledText: string): string
       - parseBooleanValue(value: string): boolean | null (returns null if invalid)

    6. Error handling: Wrap in try-catch, show user-friendly errors

    7. Minimum 60 lines (excluding imports/comments)

  </action>
  <verify>
  File exists with:
  - privacyCommand with name, aliases, description, usage
  - Handler showing current privacy settings
  - Support for "set" subcommand to change settings
  - Clear descriptions for each privacy option
  - Minimum 60 lines
  </verify>
  <done>
  /privacy command created for viewing and managing privacy configuration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add --history flag to /stats command</name>
  <files>cli/src/commands/stats.ts</files>
  <action>
    Enhance /stats command with --history flag for historical view:

    1. Add options to command:
       ```typescript
       options: [
         {
           name: "history",
           alias: "H",
           description: "Show historical session list instead of dashboard",
           type: "boolean",
           default: false
         }
       ]
       ```

    2. Update handler to check context.options.history:
       - If history flag is true:
         * Get AggregationService instance
         * Call getHistoricalSessions(20) for recent sessions
         * Format sessions as table/list via addMessage()
         * Show columns: ID, Start Time, Tokens, Commands, Exit Reason
         * If no sessions: show "No historical sessions found"
       - If history flag is false:
         * Keep existing dashboard toggle behavior

    3. Formatting approach for history:
       - Use simple text formatting (no ink-table dependency)
       - Example format:
         ```
         Recent Sessions:
         ┌──────────────┬─────────────────┬────────┬───────────┬────────────┐
         │ Session ID   │ Start Time      │ Tokens │ Commands  │ Exit Reason │
         ├──────────────┼─────────────────┼────────┼───────────┼────────────┤
         │ abc-123-def  │ 2025-01-17 10:30│ 12,345 │ 23        │ completed  │
         └──────────────┴─────────────────┴────────┴───────────┴────────────┘
         ```

    4. Ensure file minimum 60 lines (will grow with history formatting)

    5. Keep existing dashboard behavior intact when --history is not set

  </action>
  <verify>
  1. Check options array includes --history flag
  2. Check handler has conditional for history mode
  3. Check AggregationService integration for getHistoricalSessions()
  4. Check formatted output via addMessage()
  5. File minimum 60 lines
  </verify>
  <done>
  /stats command now supports --history flag for viewing past sessions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Register privacy command</name>
  <files>cli/src/commands/index.ts</files>
  <action>
    Add privacy command to command registry:

    1. Import at top:
       ```typescript
       import { privacyCommand } from "./privacy.js"
       ```

    2. Add to initializeCommands():
       ```typescript
       commandRegistry.register(privacyCommand)
       ```
       Place near other settings commands (config, profile)

    3. No other changes needed

  </action>
  <verify>
  1. Check import statement added
  2. Check commandRegistry.register(privacyCommand) in initializeCommands()
  3. Build: pnpm --filter @kilocode/cli build
  </verify>
  <done>
  Privacy command registered and available in CLI.
  </done>
</task>

</tasks>

<verification>
1. Build: pnpm --filter @kilocode/cli build
   Expected: Successful build, no TypeScript errors

2. Lint: pnpm --filter @kilocode/cli lint
   Expected: No new linting errors

3. Manual verification (after execution):
    - Run /privacy - should show current settings
    - Run /privacy set hashPII false - should update setting
    - Run /stats --history - should show session list
      </verification>

<success_criteria>

- /privacy command shows current settings clearly
- /privacy set can modify privacy configuration
- /stats --history shows historical sessions
- Privacy changes persist to MetricsCollectorService
- Both commands handle errors gracefully
  </success_criteria>

<output>
After completion, create `.planning/phases/05-reports-polish/05-03-SUMMARY.md`
</output>
