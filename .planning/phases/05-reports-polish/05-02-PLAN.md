---
phase: 05-reports-polish
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
    - cli/src/commands/export.ts
    - cli/src/commands/__tests__/export.test.ts
    - cli/src/commands/index.ts
autonomous: true

must_haves:
    truths:
        - "User can run /export command to download analytics data"
        - "Export saves to file system with default location"
        - "Export command shows success message with file path"
        - "Command respects --output option for custom file paths"
    artifacts:
        - path: "cli/src/commands/export.ts"
          provides: "/export command for JSON data export"
          min_lines: 50
        - path: "cli/src/commands/__tests__/export.test.ts"
          provides: "Test coverage for export command"
          min_lines: 40
        - path: "cli/src/commands/index.ts"
          provides: "Command registration"
          exports: ["initializeCommands"]
    key_links:
        - from: "export.ts"
          to: "ReportGenerator"
          via: "getInstance() for export methods"
          pattern: "ReportGenerator\\.getInstance\\(\\)"
        - from: "export.ts"
          to: "CommandRegistry"
          via: "register() in index.ts"
          pattern: "commandRegistry\\.register\\(exportCommand\\)"
---

<objective>
Create /export command that uses ReportGenerator to save analytics data as JSON file.

Purpose: Provide user-facing command for exporting their analytics data. Completes the "data portability" feature with CLI access.

Output: Working /export command with --output option, file system integration, success feedback.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@cli/src/commands/core/types.ts
@cli/src/commands/stats.ts
@cli/src/commands/index.ts
@cli/src/services/analytics/ReportGenerator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /export command</name>
  <files>cli/src/commands/export.ts</files>
  <action>
    Create export command following existing command pattern (see stats.ts):

    1. Command structure (implement Command interface):
       - name: "export"
       - aliases: ["download", "backup"]
       - description: "Export analytics data to JSON file"
       - usage: "/export [--output <path>]"
       - examples: ["/export", "/export --output ~/backup.json", "/download"]
       - category: "system"
       - priority: 5

    2. Add options (CommandOption[]):
       ```typescript
       options: [
         {
           name: "output",
           alias: "o",
           description: "Output file path for export",
           type: "string",
           default: path.join(os.homedir(), "kilocode-analytics-export.json")
         }
       ]
       ```

    3. Handler implementation:
       - Extract output path from context.options.output or use default
       - Get ReportGenerator instance: ReportGenerator.getInstance()
       - Call generateFullExport() to get export data
       - Call exportToJsonFile(exportData, outputPath)
       - Show success message via context.addMessage():
         * "Export complete: {outputPath}"
         * Include file size in human-readable format
         * Include session count

    4. Error handling:
       - Wrap in try-catch
       - On error: show user-friendly error message via addMessage()
       - Do NOT throw - handle gracefully

    5. Import required modules:
       - ReportGenerator from services/analytics/ReportGenerator
       - path, fs from Node.js

    6. Minimum 50 lines (excluding imports/comments)

  </action>
  <verify>
  File exists with:
  - exportCommand with name, aliases, description, usage, examples
  - options array with --output option
  - Handler that calls ReportGenerator methods
  - Success/error message handling via addMessage()
  - Minimum 50 lines
  </verify>
  <done>
  /export command created with file output, option support, and user feedback.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add export command tests</name>
  <files>cli/src/commands/__tests__/export.test.ts</files>
  <action>
    Create tests for export command:

    1. Mock CommandContext:
       - Use pattern from stats.test.ts
       - Mock addMessage, options, other context properties

    2. Test scenarios (minimum 4 test cases):
       - Command exports to default path when no option provided
       - Command exports to custom path with --output option
       - Command shows success message with file path
       - Command handles errors gracefully

    3. Mock ReportGenerator:
       - vi.fn() for getInstance()
       - Mock generateFullExport() to return test data
       - Mock exportToJsonFile() to simulate file write

    4. Verify:
       - Correct output path passed to exportToJsonFile
       - addMessage called with success/error message
       - Options parsed correctly from context

    5. Minimum 40 lines of test code

  </action>
  <verify>
  Run: pnpm --filter @kilocode/cli test export
  Expected: All tests pass, minimum 4 test cases, 40+ lines
  </verify>
  <done>
  Export command has test coverage for happy path and error cases.
  </done>
</task>

<task type="auto">
  <name>Task 3: Register export command in index.ts</name>
  <files>cli/src/commands/index.ts</files>
  <action>
    Add export command to command registry:

    1. Import exportCommand at top of file:
       ```typescript
       import { exportCommand } from "./export.js"
       ```

    2. Add to initializeCommands() function:
       ```typescript
       commandRegistry.register(exportCommand)
       ```
       Place after statsCommand registration for logical grouping

    3. No other changes needed - command will be available to users

  </action>
  <verify>
  1. Check import statement added for exportCommand
    2. Check commandRegistry.register(exportCommand) in initializeCommands()
    3. Build: pnpm --filter @kilocode/cli build
       Expected: No build errors
  </verify>
  <done>
  Export command registered and available in CLI.
  </done>
</task>

</tasks>

<verification>
1. Run: pnpm --filter @kilocode/cli test export
   Expected: All tests pass

2. Build: pnpm --filter @kilocode/cli build
   Expected: Successful build with no TypeScript errors

3. Lint: pnpm --filter @kilocode/cli lint
   Expected: No new linting errors
   </verification>

<success_criteria>

- /export command works from CLI
- Default output path: ~/kilocode-analytics-export.json
- --output option for custom paths
- Success messages shown to user
- Error handling works
- Command registered in registry
  </success_criteria>

<output>
After completion, create `.planning/phases/05-reports-polish/05-02-SUMMARY.md`
</output>
