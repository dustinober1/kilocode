---
phase: 03-state-aggregation
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
    - cli/src/services/analytics/AnalyticsStateIntegration.ts
    - cli/src/services/analytics/__tests__/AnalyticsStateIntegration.test.ts
    - cli/src/cli.ts
autonomous: true

must_haves:
    truths:
        - MetricsCollector 'flush' event triggers debounced atom refresh
        - MetricsCollector 'session:end' event triggers immediate atom refresh
        - Cache invalidation happens on flush for current session
        - initializeAnalyticsStateIntegration() called during CLI startup
        - Atoms update in real-time as events occur
        - No render thrashing from high-frequency events (100ms debounce)

    artifacts:
        - path: cli/src/services/analytics/AnalyticsStateIntegration.ts
          provides: "EventEmitter to atom bridge for real-time updates"
          min_lines: 60
          contains: "function initializeAnalyticsStateIntegration"
        - path: cli/src/cli.ts
          provides: "CLI entry point with analytics integration initialized"
          contains: "initializeAnalyticsStateIntegration"
        - path: cli/src/services/analytics/__tests__/AnalyticsStateIntegration.test.ts
          provides: "Tests for event-to-atom wiring"
          min_lines: 80

    key_links:
        - from: "AnalyticsStateIntegration.ts"
          to: "MetricsCollectorService.ts"
          via: "MetricsCollector.getInstance().on('flush', ...)"
          pattern: "metrics\\.on\\('flush'"
        - from: "AnalyticsStateIntegration.ts"
          to: "analytics.ts atoms"
          via: "import { debouncedRefreshAtom } from state/atoms/analytics"
          pattern: "from.*atoms/analytics"
        - from: "cli.ts"
          to: "AnalyticsStateIntegration.ts"
          via: "initializeAnalyticsStateIntegration() call in initialize()"
          pattern: "initializeAnalyticsStateIntegration"
---

<objective>
Wire MetricsCollector events to trigger atom updates with debouncing and cache invalidation.

Purpose: Keep UI state synchronized with database writes by listening to MetricsCollector events and triggering reactive atom updates.
Output: AnalyticsStateIntegration module that bridges EventEmitter to Jotai atoms, initialized during CLI startup.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-state-aggregation/03-RESEARCH.md
@.planning/phases/02-data-collection-layer/02-03-SUMMARY.md
@.planning/phases/03-state-aggregation/03-01-SUMMARY.md
@.planning/phases/03-state-aggregation/03-02-SUMMARY.md
@cli/src/cli.ts
@cli/src/services/analytics/MetricsCollectorService.ts
@cli/src/state/atoms/analytics.ts
@cli/src/services/analytics/QueryCache.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AnalyticsStateIntegration module</name>
  <files>cli/src/services/analytics/AnalyticsStateIntegration.ts</files>
  <action>
    Create AnalyticsStateIntegration.ts with event-to-atom bridge:

    1. Import MetricsCollector, aggregationCache, and jotai store:
       import MetricsCollector from "./MetricsCollectorService"
       import { aggregationCache } from "./QueryCache"
       import { getDefaultStore } from "jotai"

    2. Create initializeAnalyticsStateIntegration function:
       export function initializeAnalyticsStateIntegration(): void {
         const metrics = MetricsCollector.getInstance()
         const store = getDefaultStore()

         // Listen for batch flush events
         metrics.on("flush", async () => {
           const sessionId = await metrics.getCurrentSessionId()
           if (sessionId) {
             // Invalidate cache for this session
             aggregationCache.invalidate(sessionId)
             // Trigger debounced refresh
             store.set(debouncedRefreshAtom)
           }
         })

         // Listen for session end events
         metrics.on("session:end", async (sessionId: string) => {
           // Invalidate cache for this session
           aggregationCache.invalidate(sessionId)
           // Trigger immediate refresh for final metrics
           store.set(refreshAnalyticsAtom)
         })
       }

    3. Export function, no default export

    Use getDefaultStore() from jotai for setting atoms outside React.
    Import debouncedRefreshAtom and refreshAnalyticsAtom from state/atoms/analytics.
    Handle empty sessionId gracefully (skip cache invalidation).

  </action>
  <verify>
    File exists at cli/src/services/analytics/AnalyticsStateIntegration.ts with initializeAnalyticsStateIntegration function
  </verify>
  <done>
    Event-to-atom integration module created
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate state initialization in CLI</name>
  <files>cli/src/cli.ts</files>
  <action>
    Update cli.ts to initialize analytics state integration:

    1. Import initializeAnalyticsStateIntegration:
       import { initializeAnalyticsStateIntegration } from "./services/analytics/AnalyticsStateIntegration"

    2. In CLI class initialize() method, after session start:
       // Initialize analytics state integration
       initializeAnalyticsStateIntegration()

    Place this after MetricsCollector.getInstance().startSession(sessionId) call.
    Wrap in try-catch to prevent analytics errors from breaking CLI startup:
       try {
         initializeAnalyticsStateIntegration()
       } catch (error) {
         console.error("Failed to initialize analytics state integration:", error)
       }

    Follow existing pattern for MetricsCollector integration (non-blocking with try-catch).

  </action>
  <verify>
    cli.ts updated with initializeAnalyticsStateIntegration() call in initialize() method
  </verify>
  <done>
    Analytics state integration initialized during CLI startup
  </done>
</task>

<task type="auto">
  <name>Task 3: Add integration tests</name>
  <files>cli/src/services/analytics/__tests__/AnalyticsStateIntegration.test.ts</files>
  <action>
    Create integration tests for event-to-atom wiring:

    1. Test flush event triggers cache invalidation:
       - Mock MetricsCollector.getInstance() to return mock instance
       - Mock aggregationCache.invalidate
       - Emit 'flush' event
       - Verify invalidate called with current session ID
       - Verify debouncedRefreshAtom set in jotai store

    2. Test session:end event triggers immediate refresh:
       - Emit 'session:end' event with sessionId
       - Verify invalidate called with session ID
       - Verify refreshAnalyticsAtom set in jotai store

    3. Test empty sessionId skips cache invalidation:
       - Mock getCurrentSessionId() to return ""
       - Emit 'flush' event
       - Verify invalidate NOT called
       - Verify debouncedRefreshAtom still set

    4. Test debounced refresh waits 100ms:
       - Use vi.useFakeTimers()
       - Emit 'flush' event
       - Advance time by 99ms, verify refresh NOT called
       - Advance time by 1ms, verify refresh called

    5. Test multiple flush events reset debounce:
       - Emit 'flush' event
       - Advance time by 50ms
       - Emit another 'flush' event
       - Advance time by 50ms, verify refresh NOT called
       - Advance time by 50ms, verify refresh called

    Use vitest patterns from integration.test.ts.
    Use getDefaultStore().get() to verify atom values.

  </action>
  <verify>
    Test file exists with 5+ test cases covering flush, session:end, debounce behavior, and edge cases
  </verify>
  <done>
    Comprehensive integration tests verify event-to-atom wiring works correctly
  </done>
</task>

</tasks>

<verification>
- initializeAnalyticsStateIntegration only called once during CLI initialization
- 'flush' event invalidates cache and triggers debounced refresh
- 'session:end' event invalidates cache and triggers immediate refresh
- Cache invalidation uses aggregationCache.invalidate(sessionId)
- Atoms updated via getDefaultStore().set() for non-React context
- Debounce delay is 100ms as specified
- Multiple flush events reset debounce timer
- Tests cover all event types and edge cases
</verification>

<success_criteria>

- AnalyticsStateIntegration module bridges MetricsCollector events to Jotai atoms
- CLI initializes integration during startup
- Atoms update in real-time as events occur
- 100ms debounce prevents render thrashing
- Cache invalidation keeps data fresh
- Integration tests verify correct behavior
  </success_criteria>

<output>
After completion, create `.planning/phases/03-state-aggregation/03-03-SUMMARY.md`
</output>
