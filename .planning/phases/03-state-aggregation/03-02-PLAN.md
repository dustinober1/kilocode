---
phase: 03-state-aggregation
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
    - cli/src/state/atoms/analytics.ts
    - cli/src/state/atoms/index.ts
autonomous: true

must_haves:
    truths:
        - currentSessionIdAtom tracks active session ID
        - sessionMetricsAtom derives metrics from current session
        - tokenUsageTimelineAtom derives token usage with running totals
        - historicalSessionsAtom derives session list
        - tokensPerMinuteAtom derives time-bucketed token usage
        - refreshAnalyticsAtom is write-only for triggering updates
        - debouncedRefreshAtom debounces updates with 100ms delay
        - All atoms follow Jotai patterns from existing codebase

    artifacts:
        - path: cli/src/state/atoms/analytics.ts
          provides: "Jotai atoms for analytics state"
          min_lines: 100
          contains: "export.*Atom"
        - path: cli/src/state/atoms/index.ts
          provides: "Central export of all atoms including new analytics atoms"
          contains: "from ./analytics.js"

    key_links:
        - from: "analytics.ts atoms"
          to: "AggregationService.ts"
          via: "import from AggregationService"
          pattern: "from.*AggregationService"
        - from: "sessionMetricsAtom"
          to: "currentSessionIdAtom"
          via: "get(currentSessionIdAtom)"
          pattern: "get\\(currentSessionIdAtom\\)"
        - from: "debouncedRefreshAtom"
          to: "refreshAnalyticsAtom"
          via: "setTimeout calling set(refreshAnalyticsAtom)"
          pattern: "setTimeout.*refresh"
---

<objective>
Create Jotai atoms for reactive analytics state with derived atoms and debounced refresh.

Purpose: Transform database queries into reactive state that UI components can consume with automatic updates.
Output: analytics.ts atom file with base atoms, derived atoms, and write-only refresh atoms following existing patterns.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-state-aggregation/03-RESEARCH.md
@.planning/phases/03-state-aggregation/03-01-SUMMARY.md
@cli/src/state/atoms/service.ts
@cli/src/state/atoms/index.ts
@cli/src/services/analytics/AggregationService.ts
@cli/src/services/analytics/MetricsCollectorService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create base and derived analytics atoms</name>
  <files>cli/src/state/atoms/analytics.ts</files>
  <action>
    Create cli/src/state/atoms/analytics.ts with Jotai atoms:

    1. Base atom for current session ID:
       const currentSessionIdAtom = atom<string>("")

    2. Read-only derived atom for session metrics:
       export const sessionMetricsAtom = atom(async (get) => {
         const sessionId = get(currentSessionIdAtom)
         if (!sessionId) return null
         const aggregation = AggregationService.getInstance()
         return await aggregation.getSessionMetrics(sessionId)
       })

    3. Read-only derived atom for token usage timeline:
       export const tokenUsageTimelineAtom = atom(async (get) => {
         const sessionId = get(currentSessionIdAtom)
         if (!sessionId) return []
         const aggregation = AggregationService.getInstance()
         return await aggregation.getTokenUsageTimeline(sessionId)
       })

    4. Read-only derived atom for historical sessions:
       export const historicalSessionsAtom = atom(async () => {
         const aggregation = AggregationService.getInstance()
         return await aggregation.getHistoricalSessions(20)
       })

    5. Read-only derived atom for tokens per minute:
       export const tokensPerMinuteAtom = atom(async (get) => {
         const sessionId = get(currentSessionIdAtom)
         if (!sessionId) return []
         const aggregation = AggregationService.getInstance()
         return await aggregation.getTokensPerMinute(sessionId)
       })

    Import atom from "jotai" and AggregationService from "../services/analytics/AggregationService".
    Follow TypeScript typing patterns from service.ts.
    Return default values (null, []) when no session ID.

  </action>
  <verify>
    File exists with 5 atoms exported: currentSessionIdAtom, sessionMetricsAtom, tokenUsageTimelineAtom, historicalSessionsAtom, tokensPerMinuteAtom
  </verify>
  <done>
    Base and derived analytics atoms created following existing patterns
  </done>
</task>

<task type="auto">
  <name>Task 2: Create write-only refresh and debounced atoms</name>
  <files>cli/src/state/atoms/analytics.ts</files>
  <action>
    Add refresh atoms to analytics.ts:

    1. Write-only atom to trigger refresh:
       export const refreshAnalyticsAtom = atom(null, async (get, set) => {
         // Force re-computation by updating currentSessionIdAtom with same value
         const currentId = get(currentSessionIdAtom)
         set(currentSessionIdAtom, currentId)
       })

    2. Debounced refresh atom with 100ms delay:
       let refreshTimeout: NodeJS.Timeout | null = null

       export const debouncedRefreshAtom = atom(null, async (get, set) => {
         if (refreshTimeout) {
           clearTimeout(refreshTimeout)
         }
         refreshTimeout = setTimeout(async () => {
           set(refreshAnalyticsAtom)
           refreshTimeout = null
         }, 100)
       })

    Follow write-only atom pattern from service.ts (atom(null, (get, set, payload) => {...})).
    Store refreshTimeout variable outside atom definition to persist across calls.
    Clear existing timeout before scheduling new one.

  </action>
  <verify>
    File updated with refreshAnalyticsAtom and debouncedRefreshAtom exported
  </verify>
  <done>
    Write-only refresh atoms created for triggering atom updates
  </done>
</task>

<task type="auto">
  <name>Task 3: Export analytics atoms from index.ts</name>
  <files>cli/src/state/atoms/index.ts</files>
  <action>
    Add analytics section to cli/src/state/atoms/index.ts:

    // ============================================================================
    // Analytics Atoms - Session metrics and aggregation state
    // ============================================================================
    export {
      // Core analytics atoms
      currentSessionIdAtom,
      sessionMetricsAtom,
      tokenUsageTimelineAtom,
      historicalSessionsAtom,
      tokensPerMinuteAtom,

      // Analytics action atoms
      refreshAnalyticsAtom,
      debouncedRefreshAtom,
    } from "./analytics.js"

    Insert this section after the existing sections (after UI Atoms section, before Type Re-exports).
    Use exact formatting from existing sections (comment header, export block, imports).
    Add to appropriate location in alphabetically-organized sections.

  </action>
  <verify>
    index.ts updated with new Analytics Atoms section exporting all 7 atoms
  </verify>
  <done>
    Analytics atoms exported from central index for easy importing
  </done>
</task>

</tasks>

<verification>
- All atoms follow existing patterns from service.ts (derived atoms, write-only atoms)
- Base atom (currentSessionIdAtom) is writable, derived atoms are read-only
- Derived atoms use async (get) => {...} pattern for database queries
- Write-only atoms use atom(null, (get, set) => {...}) pattern
- Debounced atom uses module-scoped variable for timeout persistence
- All atoms handle null/empty session ID gracefully
- TypeScript types inferred or explicitly defined
- No side effects in derived atoms (only in write-only atoms)
</verification>

<success_criteria>

- 7 atoms created and exported from analytics.ts
- All atoms exported from central index.ts
- Atoms follow existing codebase patterns
- Ready for MetricsCollector integration in Plan 03
  </success_criteria>

<output>
After completion, create `.planning/phases/03-state-aggregation/03-02-SUMMARY.md`
</output>
